#include <iostream>
using namespace std;

int main(){
    long n,k,t;
    long ans=0;
    cin>>n>>k;
    while(n--)
    {
        cin>>t;
        if(t%k==0)
            ans++;
    }
    cout<<ans;
    return 0;
}
